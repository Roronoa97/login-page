import './button.css'

const Button = () => {
    return (
        <div className="btn-wrapper">
            <button className="btn btn-primary">
                Sign Up
            </button>
        </div>
    )
}

export default Button;